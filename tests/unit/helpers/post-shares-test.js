import { postShares } from 'embeter-materialize/helpers/post-shares';
import { module, test } from 'qunit';

module('Unit | Helper | post shares');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = postShares([42]);
  assert.ok(result);
});
