import { postIsfollowing } from 'embeter-materialize/helpers/post-isfollowing';
import { module, test } from 'qunit';

module('Unit | Helper | post isfollowing');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = postIsfollowing([42]);
  assert.ok(result);
});
