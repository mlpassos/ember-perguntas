import { postReactions } from 'embeter-materialize/helpers/post-reactions';
import { module, test } from 'qunit';

module('Unit | Helper | post reactions');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = postReactions([42]);
  assert.ok(result);
});
