import { postClass } from 'embeter-materialize/helpers/post-class';
import { module, test } from 'qunit';

module('Unit | Helper | post class');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = postClass([42]);
  assert.ok(result);
});
