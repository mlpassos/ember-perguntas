import { comparePosts } from 'embeter-materialize/helpers/compare-posts';
import { module, test } from 'qunit';

module('Unit | Helper | compare posts');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = comparePosts([42]);
  assert.ok(result);
});
