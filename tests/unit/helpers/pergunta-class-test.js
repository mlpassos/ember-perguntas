import { perguntaClass } from 'embeter-materialize/helpers/pergunta-class';
import { module, test } from 'qunit';

module('Unit | Helper | pergunta class');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = perguntaClass([42]);
  assert.ok(result);
});
