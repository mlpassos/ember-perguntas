import { userInfo } from 'embeter-materialize/helpers/user-info';
import { module, test } from 'qunit';

module('Unit | Helper | user info');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = userInfo([42]);
  assert.ok(result);
});
