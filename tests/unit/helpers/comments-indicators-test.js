import { commentsIndicators } from 'embeter-materialize/helpers/comments-indicators';
import { module, test } from 'qunit';

module('Unit | Helper | comments indicators');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = commentsIndicators([42]);
  assert.ok(result);
});
