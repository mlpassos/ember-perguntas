<div class="container">
	<div class="row">
	  	{{#if session.isAuthenticated}}
	  		{{post-view post=model postUsers=postUsers session=session}}
	    {{else}}
	    	Você precisa estar logado para ver as notícias
	    {{/if}}
	</div>
</div>
{{outlet}}